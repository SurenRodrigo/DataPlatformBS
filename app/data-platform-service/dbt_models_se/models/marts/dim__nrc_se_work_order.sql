SELECT
    {{ dbt_utils.generate_surrogate_key(['id']) }} AS work_order_sk,
    id,
    work_order_no,
    name,
    description,
    project_id,
    project_number,
    project_name,
    customer_id,
    customer_name,
    customer_type_name,
    customer_contact_id,
    customer_contact_name,
    customer_reference,
    status_id,
    status_code,
    status_name,
    foreman_id,
    foreman_name,
    service_man_id,
    service_man_name,
    price_type_id,
    work_pool_group_id,
    district,
    salary_no,
    report_sign,
    order_date,
    production_start,
    production_end,
    is_closed,
    is_reminder,
    is_credit_risk,
    is_sign_on_site,
    has_document,
    is_contractor_vat,
    is_fully_invoiced,
    is_include_on_offer,
    earned_revenue,
    total_work_cost,
    calculated_cost,
    booked_cost_total,
    budget_hours_total,
    booked_hours_total,
    total_work_revenue,
    calculated_revenue,
    booked_revenue_total,
    contribution_margin,
    contribution_margin_percent,
    total_earned_not_invoiced,
    created_at,
    last_modified_at
FROM {{ ref('stg__next_erp_work_order') }}

