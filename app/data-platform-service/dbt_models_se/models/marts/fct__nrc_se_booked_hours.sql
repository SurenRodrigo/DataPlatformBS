SELECT
    {{ dbt_utils.generate_surrogate_key(['id', 'date', 'user_id']) }} AS booked_hours_sk,
    id,
    date,
    user_id,
    employee_no,
    full_name,
    project_id,
    project_no,
    project_name,
    project_manager_name,
    work_order_id,
    work_order_no,
    work_order_name,
    customer_id,
    cost_center_id,
    employment_type_id,
    profession_item_id,
    profession_code,
    profession_salary_no,
    profession_account_no,
    hours,
    start_time,
    stop_time,
    break_time,
    cost_unit,
    price_unit,
    total_cost,
    total_revenue,
    travel_on_duty_cost,
    travel_on_duty_price,
    travel_private_cost,
    allowance_unit_cost,
    allowance_unit_price,
    is_debit,
    is_locked,
    is_addition,
    is_approved,
    is_attested,
    is_invoiced,
    is_absence,
    is_piecework,
    attest_time,
    attest_user_id,
    attest_full_name,
    diary_note,
    created_at,
    last_modified_at
FROM {{ ref('stg__next_erp_booked_hours') }}

