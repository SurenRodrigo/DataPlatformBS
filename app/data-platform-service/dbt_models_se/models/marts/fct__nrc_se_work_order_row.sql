SELECT
    {{ dbt_utils.generate_surrogate_key(['id', 'work_order_id']) }} AS work_order_row_sk,
    id,
    work_order_id,
    description,
    code_no,
    comment,
    account_no,
    invoice_number,
    article_category_id,
    article_category_name,
    item_unit_id,
    item_unit_name,
    quantity,
    used_quantity,
    planned_quantity,
    cost_unit,
    price_unit,
    total_cost,
    total_price,
    tender_amount,
    cost_unit_price_list,
    price_unit_price_list,
    payoff_unit_price_list,
    booked_hours_id,
    credit_project_id,
    performed_date,
    performed_user_id,
    performed_user_name,
    is_addition,
    is_locked,
    is_payoff,
    is_chargeable,
    is_invoiced,
    is_show_in_mobile,
    created_at,
    last_modified_at
FROM {{ ref('stg__next_erp_work_order_row') }}

